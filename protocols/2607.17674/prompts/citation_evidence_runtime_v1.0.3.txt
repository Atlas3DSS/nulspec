Runtime presentation note: `source_chunk.extracted_pages` is the complete model-facing view of this chunk's acquired text. Each entry's `page_number` is authoritative and its `text` is an exact source substring. Copy every evidence excerpt directly from one entry's `text` and use that same entry's `page_number`; never infer a page from a printed header, section number, or position in the document. PDF line wrapping may split a word with a retained hyphen, such as `identi-` followed by `fiability` on the next line. Do not silently dehyphenate or otherwise repair source text. Whitespace may collapse under validation, but hyphen characters may not. Prefer a shorter exact substring when line wrapping would make a quote awkward.
