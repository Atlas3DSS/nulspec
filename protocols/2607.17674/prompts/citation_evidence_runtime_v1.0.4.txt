Runtime presentation note: `source_chunk.extracted_pages[].source_lines` is the complete model-facing view of this chunk's acquired text. Each array item is one exact physical PDF-extraction line, including any retained line terminator; its one-based line number is its position in that array, and its parent `page_number` is authoritative. Every evidence excerpt must be copied from a substring of exactly one source-line string; never combine adjacent lines or reconstruct a sentence across lines. If a line ends with a split-word hyphen, prefer a shorter interior phrase that ends before that hyphen. Preserve every character exactly apart from whitespace normalization. If no single source line contains relevant evidence for an occurrence, return no evidence for this chunk instead of repairing or joining the text.
