# Replication error ledger

Local mistakes and upstream/release issues are kept separate. A failed command
is not promoted into a scientific finding unless independent evidence supports
it.

## NULSPEC/local errors

| ID | Time (PDT) | What happened | Result impact | Resolution |
|---|---|---|---|---|
| SPRKD-LOCAL-001 | 2026-07-31 | First source-tar extraction used a path relative to the wrong working directory. | No source was modified and no result was produced. | Re-ran against the resolved path; hashes were preserved. |
| SPRKD-LOCAL-002 | 2026-07-31 | A convenience environment install began resolving a duplicate 526 MB Torch build despite an existing CUDA stack. | Installation was interrupted before commit; no experiment ran in that state. | Reused the existing Torch 2.9.1 CUDA 13 stack and isolated only missing packages. |
| SPRKD-LOCAL-003 | 2026-07-31 | Two exploratory evaluation commands used incorrect relative paths. | Both stopped before model evaluation; no metric was inferred. | Re-ran using resolved paths. |
| SPRKD-LOCAL-004 | 2026-07-31 | An exploratory metric-summary command assumed every artifact had validation entries; the RKD trace has none. | SPRKD/Control summaries printed before the exception remain valid; no RKD validation result was fabricated. | Analysis code treats missing series explicitly. |
| SPRKD-LOCAL-005 | 2026-07-31 | A monitoring command omitted the SSH prefix and looked for dev-box PID/log paths on the workstation. | It performed read-only checks, found nothing, and did not signal or alter either run. | Re-issued the monitor over the pinned SSH target and verified both runs plus the protected shared service. |
| SPRKD-LOCAL-006 | 2026-07-31 | Two follow-on launch safety checks had shell-field/escaping mistakes while reading `MemAvailable`. | Both failed closed before launching a process; running trials and the protected shared service were untouched. | Corrected the read to the numeric second field, rechecked the 16 GiB launch floor, then launched at low priority. |
| SPRKD-LOCAL-007 | 2026-07-31 | The first uv lock command used its secure first-index default with PyTorch's supplemental wheel index; resolution stopped when that index lacked the pinned `tqdm`. | No environment or running process changed, and no lock was written. | Re-ran with explicit cross-index resolution and generated a complete hash-pinned lock; installation still enforces those hashes. |
| SPRKD-LOCAL-008 | 2026-08-01 | A read-only syntax-check command was launched from the repository root while using study-relative paths. | The shell stopped at the missing path before running the checks; no file, process, or result changed. | Re-ran the checks from the study directory. |
| SPRKD-LOCAL-009 | 2026-08-01 | The first documentation patch for LOCAL-008 and a new author question had an invalid hunk delimiter. | The patch failed atomically; neither document changed. | Corrected the patch structure and re-applied it. |
| SPRKD-LOCAL-010 | 2026-08-01 | A lint command assumed Ruff was installed inside the study virtual environment. | The missing executable stopped that lint subcommand; prior syntax checks had passed and no file changed. | Used the pinned workstation Ruff executable; all checks and formatting checks passed. |
| SPRKD-LOCAL-011 | 2026-08-01 | The first auxiliary citation-extraction command used the system Python, which lacked Beautiful Soup. | It stopped at import time before fetching or writing a paper audit; replication results were unaffected. | Re-ran with the existing citation-audit environment after verifying its Beautiful Soup and Requests versions. |
| SPRKD-LOCAL-012 | 2026-08-01 | The citation pipeline's first resolver pass received blank title/author/year fields from a generic LaTeXML bibliography layout. | The resulting 46 unresolved records and one false mismatch were rejected before Qwen review; replication results were unaffected. | Added and tested a deterministic parser fallback, versioned the acquisition change, and regenerated all downstream audit material. |
| SPRKD-LOCAL-013 | 2026-08-01 | The regenerated citation resolver encountered sustained Semantic Scholar rate limits and used an overly short retry policy. | The pass was interrupted after three references and wrote no result manifest; replication results were unaffected. | Increased anonymous-request spacing, honored server backoff, and restarted from the regenerated citation manifest. |
| SPRKD-LOCAL-014 | 2026-08-01 | The resolver's second attempt requested five title-search candidates although only the best match was used; that request shape remained rate-limited. | It was interrupted after one reference and wrote no manifest; replication results were unaffected. | Controlled probes isolated the request-size effect; the resolver now requests one candidate and retains independent title-similarity validation. |
| SPRKD-LOCAL-015 | 2026-08-01 | A third citation-resolution launch still depended on an intermittently rate-limited title index and could not complete its first record promptly. | It was interrupted before writing a manifest or dispatching model review; replication results were unaffected. | Added an arXiv-title lookup ahead of the index and recovered plain-text bibliography URLs, then regenerated inputs. |
| SPRKD-LOCAL-016 | 2026-08-01 | A read-only PyHessian introspection tool wrapper referenced a JavaScript variable from a prior isolated tool invocation. | The wrapper stopped before launching a shell process; no experiment or file changed. | Reissued the command with an explicit working directory and captured the installed PyHessian signatures/source. |
| SPRKD-LOCAL-017 | 2026-08-01 | The arXiv-first citation pass grounded 21/25 attempted references but still stalled on older literature because the documented Crossref path was not implemented. | The pass was interrupted before committing a manifest or dispatching review; replication results were unaffected. | Implemented cached Crossref bibliographic/DOI recovery under the frozen identity threshold and restarted cleanly. |
| SPRKD-LOCAL-018 | 2026-08-01 | The one-record arXiv resolver smoke output placed a correct arXiv URL in the legacy `semantic_scholar_url` identity field. | No model consumed the smoke record and no finding was produced. | Added provider-neutral catalog fields, restricted the legacy field to Semantic Scholar records, and invalidated the smoke output. |
| SPRKD-LOCAL-019 | 2026-08-01 | The six-record follow-up smoke reused pre-v1.4 arXiv cache objects that lacked the new provider tag, so four correct arXiv identities still serialized with the legacy provider label. | Source matches, evidence objects, and replication results were unaffected; no model consumed the smoke output. | Added deterministic provider tagging when old arXiv/Crossref cache records are read, invalidated the smoke output, and repeated the test before full resolution. |
| SPRKD-LOCAL-020 | 2026-08-01 | A read-only smoke-summary snippet assumed citation records were under a top-level `citations` key rather than the manifest's `audits` key. Because the shell sequence lacked fail-fast mode, the intended resolver smoke still ran after the snippet raised `KeyError`. | The summary snippet wrote nothing; the resolver output was independently inspected under the correct schema before use. | Switched subsequent validation to `audits` and explicit assertions. |
| SPRKD-LOCAL-021 | 2026-08-01 | The conservative worker-promotion watcher sent `SIGINT` to the validated seed-4 parent, but the training process did not exit within its 30-second limit, so the watcher correctly refused a stronger fallback signal. | Four iterations of the next uncheckpointed stage ran at the old loader count; no completed checkpoint or reported result changed. | Revalidated the exact parent command, shared-service health, RAM, temperature, and child set; sent `SIGTERM`, confirmed the whole seed process tree stopped, and resumed from the completed exact-script boundary with six workers. |
| SPRKD-LOCAL-022 | 2026-08-01 | The first 47-item citation packet build used the generic 50,000-character evidence allowance; two serialized packets exceeded the batcher's 65,000-character total-input ceiling after citation contexts and metadata were added. | Batching failed closed before Qwen dispatch, so no trace or finding used the oversized packet set. | Rebuilt every packet uniformly with a 40,000-character source-passage allowance, revalidated all hashes and counts, then generated the schedule. |
| SPRKD-LOCAL-023 | 2026-08-01 | A post-build assertion expected exactly five cross-GPU duplicate citations and 52 jobs, while the frozen deterministic one-in-ten hash predicate selected four duplicates and 51 jobs for this paper. | The assertion ran after—and did not modify—the valid schedule; no model had been dispatched. | Inspected the predicate and manifest directly, confirmed 47 primary plus four duplicate jobs and both physical endpoint assignments, and retained the generated schedule unchanged. |
| SPRKD-LOCAL-024 | 2026-08-01 | A read-only remote status snippet used single quotes inside an already single-quoted SSH command, so Python evaluated `pid` as a variable and raised `NameError` after printing the requested log tail. | The monitor wrote nothing and did not signal a process; the relevant stage and watcher evidence had already printed. | Removed nested quote ambiguity from subsequent remote snippets. |
| SPRKD-LOCAL-025 | 2026-08-01 | The same conservative `SIGINT` promotion limit described in LOCAL-021 was reached for seed 3 after five iterations of its next uncheckpointed stage. | No completed checkpoint or reported result changed. | Repeated the exact process/service/RAM/temperature validation, used `SIGTERM`, confirmed the seed-3 process tree stopped, and resumed from the completed exact-script checkpoint with six workers. |
| SPRKD-LOCAL-026 | 2026-08-01 | Pre-dispatch citation-trace review found that the batch runner retained the served model alias and raw API usage but not the exact GGUF digest promised by the audit method. | No model request had been sent for this paper, so no trace required migration and replication outcomes were unaffected. | Made a matching per-endpoint SHA-256 mandatory, added it to trace identity/reuse checks and manifests, and exposed raw token usage in a stable attempt field. |
| SPRKD-LOCAL-027 | 2026-08-01 | The first Qwen server launch on the PRO 6000 used the launcher's default Ampere-compatible llama.cpp binary; model loading succeeded, but warm-up aborted with `no kernel image is available for execution on the device`. | No inference request or citation trace ran; the server process exited and released its memory. Base training and the protected shared service remained healthy. | Located and verified the existing separate `sm_120` build, relaunched with that explicit binary, and passed the health/model/resource checks. |
| SPRKD-LOCAL-028 | 2026-08-01 | A read-only toolchain inventory used fail-fast mode and assumed `cmake` was on the remote system PATH. | The command stopped after reporting CUDA 13.1; it changed nothing and did not affect running jobs. | Reissued a non-mutating search, found the repository's isolated build environment and pre-existing Blackwell build, and avoided an unnecessary rebuild. |
| SPRKD-LOCAL-029 | 2026-08-01 | A read-only early-trace summary tried to print a nonexistent generic `rationale` field instead of the schema's `reasoning_summary`. | It stopped after displaying the first trace's mechanical evidence errors; no trace or request changed. | Reissued schema-tolerant inspection and confirmed that structural validation, repair attempts, raw usage, and evidence-error retention were operating as designed. |
| SPRKD-LOCAL-030 | 2026-08-01 | One unusually context-heavy citation exhausted the 900-token Qwen cap on both initial and repair attempts, leaving truncated invalid JSON. | The failed trace is an orchestration example, not a model-quality grade; no citation verdict was accepted from it. | Retained the failed trace, added an explicit 1,800-token second-attempt cap only after `finish_reason=length`, tightened trace reuse to the frozen base cap, and queued the citation for clean rerun. |
| SPRKD-LOCAL-031 | 2026-08-01 | Running both physical Qwen endpoints concurrently caused available shared-host RAM to dip to 11.82 GiB, just below the citation dispatch floor. | The fail-closed guard stopped new PRO batches; completed valid traces remained intact, base jobs continued, and the protected shared service stayed healthy. | Unloaded the PRO server, recovered 21 GiB available RAM, and returned to sequential physical-endpoint review before resuming pending PRO batches. |
| SPRKD-LOCAL-032 | 2026-08-01 | Pre-grading inspection found that the outer-teacher prompt received validation errors but not the observable attempted response when a batch had no schema-valid final object. | No outer-teacher grade had run for this paper; Qwen raw traces were already complete and unaffected. | Added full invalid-attempt content, parse state, finish reason, usage, and validation errors to the teacher case, with explicit model-versus-orchestration attribution rules. |
| SPRKD-LOCAL-033 | 2026-08-01 | The first patch strengthening Qwen's schema-repair instruction targeted a line layout that Ruff had already reformatted. | The patch failed atomically; running processes and trace files were untouched. | Inspected the current hunk, reapplied the same semantic change against the formatted source, and passed formatting, lint, and compilation checks. |
| SPRKD-LOCAL-034 | 2026-08-01 | The first lint pass on the new citation-audit analyzer found an unused `math` import. | The analyzer had not run on a result manifest and no output existed. | Removed the import and repeated compilation, formatting, lint, and CLI checks. |
| SPRKD-LOCAL-035 | 2026-08-01 | Pre-publication provenance review found that batch manifests pointed to full Qwen/teacher trace files but did not bind those paths to file digests. | Raw traces were already retained and no grade/result changed; final consolidated manifests had not been generated. | Added trace SHA-256 fields for new and reused Qwen/teacher traces, bound each teacher trace to its Qwen digest, and made the public analyzer fail on any mismatch. |
| SPRKD-LOCAL-036 | 2026-08-01 | A read-only server-slot monitor piped JSON into a Python process whose heredoc simultaneously consumed standard input, so the decoder saw an empty stream. | It changed nothing; the Qwen request and all experiment processes continued normally. | Reissued the health/slot read using `python -c`, which preserved the pipe and confirmed the endpoint was healthy and processing. |
| SPRKD-LOCAL-037 | 2026-08-01 | A read-only log-filter command let the shell interpret sed's intended end-of-file `$p` token, producing an invalid sed expression. | The filter failed without touching the server or log. | Read the bounded log tail directly and confirmed normal prompt/generation timing. |
| SPRKD-LOCAL-038 | 2026-08-01 | Post-run review of the four cross-endpoint citation duplicates found that their primary and repeat requests could be co-batched with different neighboring packets and used endpoint-dependent batch seed keys. | The observed 50% verdict agreement is valid for the two production routes as executed, but cannot isolate a physical-GPU effect. Primary citation reviews and teacher grades are unaffected. | Preserve and report the comparison as operational route consistency, explicitly reject a hardware-causality interpretation, and require identical one-packet requests plus a shared seed key in a future strict hardware test. |
| SPRKD-LOCAL-039 | 2026-08-01 | The first citation-summary invocation ran from the study directory while its arguments were still prefixed from the repository root. | It stopped at the first missing input path before writing a result. | Re-ran from the repository root so manifest paths and the trace paths recorded inside them shared one resolution base. |
| SPRKD-LOCAL-040 | 2026-08-01 | A second citation-summary attempt corrected the direct manifest arguments but still ran from the study directory, so repository-root-relative trace paths inside the manifests could not resolve. | Trace-hash validation stopped before output; no citation result was accepted from that attempt. | Re-ran from the repository root and required all 38 Qwen and teacher trace hashes to validate before analysis. |
| SPRKD-LOCAL-041 | 2026-08-01 | A read-only inspection requested the complete citation JSON and Markdown, exceeding the tool wrapper's display allowance. | Files were not changed; the already-generated result remained intact. | Switched to bounded extraction of schema, coverage, score, verdict, attribution, and sample-finding fields. |
| SPRKD-LOCAL-042 | 2026-08-01 | After adding the public trace-hash inventory, the first regeneration command guessed a nonexistent `resolved_sources` directory for the resolved manifest. | The analyzer stopped before rewriting results; formatting, lint, and compilation of the source change had already passed. | Located the frozen manifest with `rg --files`, reran from the repository root, and asserted all 38 public inventory entries and both trace digests per batch. |
| SPRKD-LOCAL-043 | 2026-08-01 | The first local extension follow-on wrapper expected a base `run.pid` file that the original workstation launch had not created. | The wrapper exited immediately; the base seed continued unchanged and no extension started early. | Resolved and inspected the unique long-lived `run_trial.py --seed 0` parent, then waited on that explicit validated PID and the base `complete.json` boundary. |
| SPRKD-LOCAL-044 | 2026-08-01 | The first remote follow-on launch wrapper contained a Bash parameter expansion inside a JavaScript template literal, so the orchestration layer rejected it as invalid JavaScript. | No SSH command executed and no remote process or file changed. | Removed the conflicting expansion, relaunched, and confirmed all four guarded follow-on watchers were alive. |
| SPRKD-LOCAL-045 | 2026-08-01 | The first operations-note patch used a wider context hunk than the current Markdown matched. | The patch failed atomically and did not change the operations record. | Reapplied the same text with two narrowly anchored hunks and verified both insertions. |
| SPRKD-LOCAL-046 | 2026-08-01 | A read-only remote seed-1 metric printer nested single quotes through SSH and stripped the dictionary-key quotes inside an f-string. | The summary snippet raised `NameError`; the completed seed files and newly launched extension were untouched. | Synced the immutable completed seed, reran the metric read locally with unambiguous quoting, and passed the full seed-integrity validator. |
| SPRKD-LOCAL-047 | 2026-08-01 | The frozen runner retained full epoch histories and final models but not the SPRKD optimizer state or an event log for transformation, NHE, and PGD actions. This became salient when seed 1's paper-intent model dropped from 93.20% at epoch 20 to 49.78% at epoch 21. | The accuracy collapse is exactly preserved and remains a valid outcome, but this run alone cannot identify which optimizer action caused it. Retrofitting instrumentation after seeing the result would invalidate a uniform five-seed mechanism analysis. | Keep the primary run unchanged, report the diagnostic limit, and require event-level optimizer instrumentation plus failed-move checkpoint/reversion in a separately frozen follow-up. |
| SPRKD-LOCAL-048 | 2026-08-01 | The first post-hoc-register patch used a README context line that differed by one word from the current file. | The combined patch failed atomically; neither the new register nor README changed. | Reapplied the register and link against the exact current tail text. |
| SPRKD-LOCAL-049 | 2026-08-01 | A two-seed analyzer smoke test exposed that the initial analyzer delegated exact McNemar p-values to the released package's numerically unstable helper. | The temporary analysis stopped with `OverflowError`; no tracked result was written and all predictions remained unchanged. | Replaced only the analysis calculation with SciPy's stable exact binomial test, retained the package contingency-table construction, and cross-validated small-count results against the released implementation. |
| SPRKD-LOCAL-050 | 2026-08-01 | A pre-execution provenance review found that the frozen Hessian diagnostic bound `complete.json` and the test set but did not hash each of the five input model checkpoints. | No Hessian job had started, so no trace or result used the weaker config. | Added all five checkpoint SHA-256 digests to the run config and fail-closed analyzer, passed static checks, and synchronized the identical corrected runner before execution. The numerical protocol is unchanged. |
| SPRKD-LOCAL-051 | 2026-08-01 | The analogous extension config bound the base completion/config/split files but did not embed hashes for the three weak-teacher checkpoints it read; this was noticed after seeds 0 and 1 had started. | Completed base stage files are immutable under the runner and were not modified, but their at-execution identity is not embedded inside the extension config. | Do not introduce mixed provenance schemas mid-run. Publish current base checkpoint hashes through the validated base run index and extension analyzer, disclose the limitation, and require direct input hashes in the next runner version. |
| SPRKD-LOCAL-052 | 2026-08-01 | The first "no Hessian started" pre-sync check matched the local queued Bash follow-on because its command line contained the future Python command text. | The check failed closed before formatting or synchronization; the queued wrapper continued waiting and no Hessian Python process existed. | Restricted the check to Python executables, confirmed no local or remote Hessian process, then synchronized and hash-matched the corrected runner. |
| SPRKD-LOCAL-053 | 2026-08-01 | The pre-training audit correctly identified response KD's second softmax but did not separately flag that supervised SPRKD/Control training also passes terminal-Softmax probabilities to `CrossEntropyLoss`. | The omission does not invalidate the faithful run—the released behavior is exactly preserved—but delayed a relevant stability diagnostic until three seeds had completed. | Disclose the audit miss, preserve every primary outcome, and register D2 as a post-hoc one-change logit-input diagnostic that cannot affect the verdict. |
| SPRKD-LOCAL-054 | 2026-08-01 | The first combined D2 documentation patch used a multi-file context that did not match the post-hoc register exactly. | The patch failed atomically; no protocol, README, or error-ledger text changed. | Applied the register, run command, and origin-separated error entries as independently verified patches. |
| SPRKD-LOCAL-055 | 2026-08-01 | The first publication-sanitization patch included a redundant analyzer cleanup whose expected duplicate line was not present. | The multi-file patch failed atomically; no runner, guard, result, or document changed. | Removed the incorrect cleanup hunk, kept the already-correct analyzer unchanged, and applied the path/UUID parameterization independently. |
| SPRKD-LOCAL-056 | 2026-08-01 | A second combined sanitization patch mistyped one expected GPU UUID in a verification context. | The patch failed atomically before changing any guard or wrapper. Running remote copies were independent and unaffected. | Switched to one-file patches with exact local context, then re-ran shell syntax and private-metadata scans. |
| SPRKD-LOCAL-057 | 2026-08-01 | The primary and extension protocols were timestamped and frozen in the working tree before the applicable runs, but were not committed to Git before execution began. | Run configs, logs, code hashes, and the unchanged protocol files preserve the executed design, but public Git history alone cannot independently prove the preregistration timing. | Preserve the original files and timestamps, make no outcome-driven change to the primary decision rule, disclose this as a local evidentiary limitation, and require a pushed protocol commit before compute in future studies. |
| SPRKD-LOCAL-058 | 2026-08-01 | The local Hessian follow-on passed the 132-byte Git-LFS pointer at `work/upstream/TESTSET.pth` instead of the separately acquired LFS object at `work/released/TESTSET.pth`. | The runner's frozen SHA-256 gate rejected the pointer before loading a tensor, creating output, or estimating a trace. The completed base and E1/E2 results were unaffected. | Verified both file hashes, corrected the documented local command, and relaunched E3 with the hash-matching released object before any local Hessian result existed. |
| SPRKD-LOCAL-059 | 2026-08-01 | The first dry-run check of the regenerated hash lock used uv's default first-index policy even though the lock records PyPI plus the supplemental PyTorch index. | Resolution failed before installation; the active environment, running jobs, lock, and results were unchanged. | Re-ran the read-only installation plan with the same explicit cross-index policy used to generate the lock and recorded the successful result. Docker uses pip's documented index behavior rather than uv's stricter default. |
| SPRKD-LOCAL-060 | 2026-08-01 | The first documentation patch for the successful lock dry-run matched a paraphrase rather than the exact existing sentence in `TESTS.md`. | The patch failed atomically and changed no test record or code. | Read the exact local context and applied the test note at the verified section boundary. |
| SPRKD-LOCAL-061 | 2026-08-01 | The first formatting check on the new website-handoff builder found that Ruff would reflow the source. | Compilation and lint had passed, the builder had not been executed on results, and no scientific output changed. | Applied Ruff's mechanical formatter, then repeated compilation, lint, and formatting checks successfully. |
| SPRKD-LOCAL-062 | 2026-08-01 | A pre-release command audit found that README's E1/E2 example invoked an unspecified `python` and omitted the upstream package from `PYTHONPATH`. | Executed jobs used explicit validated interpreters and paths, so results were unaffected; a clean reader could have hit an import or environment mismatch. | Changed the example to the study virtual environment and explicit `work/upstream:scripts` module path, consistent with every other runner. |
| SPRKD-LOCAL-063 | 2026-08-01 | The artifact-replay requirements file documented `fastai` but included only the base replication lock, which does not contain that optional dependency. | The executed environment had fastai 2.8.8 and the retained replay result is valid, but the documented clean install could not unpickle the released historical Learner objects. | Added fastai to the human-maintained artifact inputs, generated a separate hash-pinned artifact lock, and changed the replay setup command to install that lock. |
| SPRKD-LOCAL-064 | 2026-08-01 | The first disposable clean-environment replay test pointed its Hessian argument at the current pointer-only upstream checkout instead of the separately acquired local LFS-object mirror. | Checkpoint and dataset evaluation completed, then the pointer failed at `torch.load`; no tracked result was rewritten. This was the same local path-class error already guarded in E3, not a new upstream outcome. | Reused the fully installed disposable environment, passed the hash-verified LFS mirror explicitly for checkpoints and Hessians, and repeated the complete replay before accepting the lock. The published fetch script itself performs `git lfs pull` into its upstream tree. |
| SPRKD-LOCAL-065 | 2026-08-01 | The first cleanup command for the validated disposable environment used recursive removal and was rejected by the command safety layer before execution. | The temporary environment remained intact and no user or study file changed. | Revalidated the exact `mktemp` path and its expected files, then moved that one disposable directory to the desktop trash with `gio`; it remains recoverable until the trash is emptied. |
| SPRKD-LOCAL-066 | 2026-08-01 | A pre-staging audit found that the repository-wide `*.lock` scheduler rule also ignored both dependency lockfiles. | The locks existed and had been tested locally, but an ordinary add would have omitted them from the public commit. | Added narrow study-local negations for the two named dependency locks and verified that both now appear in the candidate file set; scheduler and process locks remain ignored. |
| SPRKD-LOCAL-067 | 2026-08-01 | Pre-publication review found that the extension analyzers revalidated the base completion/config/split but did not independently revalidate the base prediction file used in paired comparisons. | No final extension aggregate had been produced, and primary results were unaffected. A later file change could otherwise have entered a paired diagnostic without detection. | Reused the primary fail-closed seed validator inside both paired extension analyzers, recorded the base prediction digest, and added target/prediction shape and binary-label checks before final analysis. |
| SPRKD-LOCAL-068 | 2026-08-01 | A read-only shared-host log scan assumed Ripgrep was installed on that host. | The monitor stopped with `rg: command not found`; no process, log, or result changed. The independent resource check and continuously running guards remained active. | Reissued the bounded scan with the host's available `grep` implementation. |
| SPRKD-LOCAL-069 | 2026-08-01 | Final provenance review found that the resumable base runner atomically rewrites `config.json` and `split_indices.pth` at every process restart before loading completed stages. | For seeds 1–4, the final config correctly records the resumed six-worker launch but does not itself preserve the initial two-worker launch. The retained logs and operations record do; the recreated split was identical, preprocessing had no random augmentation, completed stage hashes were preserved, and all scientific fields passed the frozen analyzer. No result changed, but the config alone is not a complete launch history. | Disclose both attempts and their boundary in `OPERATIONS.md`; require future runners to append immutable per-attempt launch manifests and fail on any resume drift outside explicitly permitted throughput-only fields. |
| SPRKD-LOCAL-070 | 2026-08-01 | A pre-build review of the typed website handoff found that its artifact allowlist omitted several documents reached by links from the included README and report, including the operations log, citation method, author questions, and human-readable diagnostic tables. | The handoff had not been built, so no incomplete site bundle was published and no scientific result changed. | Expanded the fail-closed allowlist to cover every linked public methods/result document plus both human dependency inputs and generated CSV/Markdown tables; the builder now refuses to run until all final diagnostics exist. |
| SPRKD-LOCAL-071 | 2026-08-01 | An operator considered moving one still-queued paired loss-contract seed to the idle workstation GPU to shorten the tail. | Pre-launch review found that this would change hardware relative to the seed's base run and violate the intended one-change pairing. Only read-only capacity/process checks had run; no training process, config, or output was created. | Rejected the migration and retained every follow-on seed on its base seed's host and physical GPU. |
| SPRKD-LOCAL-072 | 2026-08-01 | A pre-publication consistency scan found that the human frontend contract named the arm metric schema `sprkd_trial_v1`, while the typed builder emitted `sprkd_trial_accuracy_v1`. | No handoff had been built or imported. A frontend following the prose literally could otherwise reject or misroute a valid accuracy payload. | Made the human contract use the builder's more explicit `sprkd_trial_accuracy_v1` identifier and added a final cross-file schema assertion to the release checks. |
| SPRKD-LOCAL-073 | 2026-08-01 | The same pre-build review found that typed run rows projected final per-seed accuracy but omitted the per-seed loss, best/final history, elapsed time, sanitized software environment, and paired comparison objects required by the frontend contract. | No handoff had been built or imported and the complete source aggregate remained intact, so no scientific evidence was lost. The incomplete projection would have forced the frontend either to omit evidence or recompute it. | Added fail-closed run-level model and comparison projections directly from the validated aggregate, retained the compatibility accuracy map, and reject any environment that still contains a hostname or raw visible-device identifier. |
| SPRKD-LOCAL-074 | 2026-08-01 | The first formatting check after expanding the typed run projection reported that `build_website_handoff.py` required mechanical reformatting. | The check wrote nothing and the builder had not run on final data. | Applied Ruff's deterministic formatter, then repeated formatting, lint, and compilation checks. |
| SPRKD-LOCAL-075 | 2026-08-01 | A final protocol-to-artifact audit found that the Hessian analyzer validated every retained common-probe value but projected only each model's mean and sample SD into the public aggregate, despite E3 promising raw probes. | No final Hessian aggregate had been produced. The saved per-seed outputs were complete and no estimate required recomputation, but the planned public machine record would have been less auditable than specified. | Added all 100 probe values per model/seed to the fail-closed aggregate run rows; the final typed handoff now carries those validated raw values without exposing private paths. |
| SPRKD-LOCAL-076 | 2026-08-01 | The typed handoff builder required completion markers for training aggregates but did not pin the schema versions of all seven source result files or independently assert the citation target identifier. | No handoff had been built or imported. A wrong-but-structurally-similar file could otherwise have passed some downstream field checks. | Added exact schema pins for every source aggregate and a target-arXiv assertion before any projection or artifact hashing occurs. |
| SPRKD-LOCAL-077 | 2026-08-01 | The first combined patch for the schema-pin fix contained an invalid update-hunk boundary. | The patch failed atomically; neither the builder nor ledger changed. | Split the edit into correctly bounded patches and reapplied it. |
| SPRKD-LOCAL-078 | 2026-08-01 | The first schema-pin draft guessed the stability aggregate identifier as `nulspec-sprkd-training-stability-v1`; the generated analyzer schema is `nulspec-sprkd-posthoc-stability-v1`. | The builder had not run and no handoff existed. | Read the generated primary/stability schema fields directly, corrected the pin, and retained the final all-source schema assertion. |
| SPRKD-LOCAL-079 | 2026-08-01 | The first editorial patch distinguishing unweighted batch-mean best histories from sample-weighted final accuracies used a line break that did not match the current report. | The combined patch failed atomically; no report or result changed. | Re-read the exact lines and applied the clarification with narrowly matched hunks. |
| SPRKD-LOCAL-080 | 2026-08-01 | The first final release-assertion script looked for shortened `LOCAL001`-style error identifiers instead of the ledger's namespaced `SPRKD-LOCAL-001` form. | Scientific assertions through the completed result, integrity, raw-probe, code-hash, and post-hoc checks had passed; only the ledger-contiguity assertion failed, and no file was written. | Corrected the validation expression to the published identifier format, retained the fail-closed release gate, and reran the complete assertion set before staging. |
| SPRKD-LOCAL-081 | 2026-08-01 | The corrected release assertion required the report verdict in all-capital text, while the report intentionally renders the same verdict in title case. | All data, hash, handoff, artifact, and no-fake-reward assertions had passed; only this case-sensitive editorial assertion failed, and no file was written. | Changed the release check to compare the normalized verdict text case-insensitively and reran the complete gate. |
| SPRKD-LOCAL-082 | 2026-08-01 | The first combined static-check command invoked Ruff from the study virtual environment, where the project linter executable is not installed. | Python compilation completed, then the command stopped before lint, shell, JSON, and diff checks; no source or result changed. | Used the already validated host Ruff executable documented by the earlier checks and reran the full static gate. |
| SPRKD-LOCAL-083 | 2026-08-01 | A read-only candidate-file/privacy precheck requested a study-relative result path while running from the repository root. | `jq` stopped on the missing relative path before the candidate list or privacy scan ran; no file changed. | Reissued the bounded inspection from the study directory and kept the later publication scan rooted in Git's explicit candidate-file list. |
| SPRKD-LOCAL-084 | 2026-08-01 | A shortened repeat of the final release validator accidentally used loaded JSON dictionaries as keys in a Python dictionary of expected means. | Schema, completion, seed, and integrity checks passed before Python raised `TypeError`; no file changed and the earlier complete release validator remained successful. | Represented the expected model means as ordinary `(source, model, value)` rows and reran the final data/handoff gate after rebuilding the handoff hash inventory. |
| SPRKD-LOCAL-085 | 2026-08-01 | The first staged-diff whitespace check found two Markdown hard-break lines in the one-page explainer with trailing spaces. | Git's release check stopped before commit; no scientific output was affected. | Removed the unnecessary hard-break spaces, restaged the scoped study, and required the cached diff check to pass. |
| SPRKD-LOCAL-086 | 2026-08-01 | The first staging pass warned that the four generated CSV tables contained CRLF records while the repository's text policy publishes LF, which would make the handoff's pre-staging byte hashes differ from checked-out Git blobs. | No commit or handoff import occurred, and the tabular values were unchanged. | Set every CSV writer's `lineterminator` explicitly to LF, reran the four fail-closed analyzers, refreshed their executed-code hashes and artifact inventory, and verified index-versus-worktree bytes before commit. |
| SPRKD-LOCAL-087 | 2026-08-01 | A read-only status command supplied the repository-root study path while already running inside that study directory. | Git printed a missing nested-directory warning after the preceding hash and LF-byte checks had completed; no file changed. | Kept subsequent status and staging commands rooted at the repository top level with one explicit study path. |
| SPRKD-LOCAL-088 | 2026-08-01 | The repository hygiene gate interpreted an IP-shaped pinned CUDA package version on a pip-compile continuation line as an RFC1918 address. | The release stopped before commit; the dependency locks, scientific results, and privacy posture were unchanged. | Extended the checker's existing pinned-version exemption to continuation lines, added positive and negative regression tests, and reran both the privacy gate and study release checks. |
| SPRKD-LOCAL-089 | 2026-08-01 | The first regression-test gate correctly reported that the new hygiene-checker files needed Ruff formatting, then invoked pytest through a system Python without pytest installed. | Compilation and lint passed; formatting and the targeted test had not yet been accepted, and no scientific output changed. | Applied Ruff's deterministic formatter and ran the targeted test through the repository's available pytest entry point before repeating the full gate. |
| SPRKD-LOCAL-090 | 2026-08-01 | After the checker fix passed its focused tests, the full hygiene scan correctly found that the error ledger itself repeated the IP-shaped dependency version outside a pinned requirement line. | The release again stopped before commit; this was a publication-metadata issue, not a secret or scientific change. | Rephrased the ledger entry without the ambiguous numeric literal and reran the complete hygiene scan. |
| SPRKD-LOCAL-091 | 2026-08-01 | Pre-staging review found that the new hygiene regression test contained its synthetic private-address literal contiguously in source; once tracked, the checker's own repository scan would correctly reject that fixture. | The test was still untracked and no commit occurred. | Constructed both synthetic IP-shaped strings from nonmatching fragments at runtime, preserving positive and negative behavior without placing a prohibited address in the repository. |
| SPRKD-LOCAL-092 | 2026-08-01 | The first patch for that fixture expected the pre-format multiline string instead of Ruff's compact form. | The patch failed atomically; neither the test nor ledger changed. | Read the formatted source and reapplied the same runtime-fragment correction against its exact text. |
| SPRKD-LOCAL-093 | 2026-08-01 | The final repository-wide pytest run reached an unrelated pre-existing test that requires an ignored result file from the earlier SLM-RL study, which is absent in this checkout. | The suite reported 46 passes and one `FileNotFoundError`; the new hygiene tests passed, and the SPRKD code/results do not depend on that missing artifact. | Preserved the failure as observed rather than fabricating the prior result, documented the exact test and missing path class in `TESTS.md`, and retained the focused passing checks as this PR's relevant regression gate. |
| SPRKD-LOCAL-094 | 2026-08-01 | The installed GitHub CLI predates `gh pr checks --watch --interval`, so the first attempted continuous CI monitor exited on an unknown flag. | The draft PR and its running checks were unchanged. | Switched to bounded read-only polling of the PR's `statusCheckRollup` field, which is supported by the installed client. |
| SPRKD-LOCAL-095 | 2026-08-01 | The first formatting check on the new one-shot Fable gate reported that Ruff would mechanically reflow the script. | Lint, compilation, and the synthetic PASS/FAIL/HARD_FAIL contract test all passed; no review request had been made and no scientific or publication result changed. | Record this local implementation issue, apply Ruff's deterministic formatter after the gate hardening edits, and repeat every static and contract check before freezing the review packet. |
| SPRKD-LOCAL-096 | 2026-08-01 | The first combined documentation patch for the final-review gate matched a wrapped README sentence too narrowly. | The patch failed atomically, so none of its six intended documentation edits applied; no review request, result, or release state changed. | Re-read the exact document boundaries, incorporate the owner's clarified mandatory human email approval, and apply narrowly scoped patches. |
| SPRKD-LOCAL-097 | 2026-08-01 | Website-side queue validation found that the first research-authored `WEBSITE_FIX_ME.md` draft cited a 41-character hexadecimal PR-head value. | The malformed reference did not resolve, but the queue file was still untracked and neither the research handoff nor scientific record depended on it. | Acknowledge the reciprocal `RF-20260801-01` item and replace the draft reference with the exact pushed pre-review commit before committing the website queue. |
| SPRKD-LOCAL-098 | 2026-08-01 | The first custom candidate audit found that a stale draft review packet was conditionally added to the pre-review website artifact list, after which rebuilding that packet changed its digest. | The mismatch stopped the audit before commit or review. Scientific outputs were unchanged, but allowing the packet and handoff to hash each other would make the pre-review bundle non-deterministic. | Include the packet and review result in the website artifact list only after a final review result exists; the pre-review handoff now excludes the packet, breaking the digest cycle. Rebuild and re-audit the handoff and packet from scratch. |
| SPRKD-LOCAL-099 | 2026-08-01 | The corrected candidate audit then guessed that the three external limitations used the namespace `SPRKD-EXTERNAL`; the ledger's actual frozen namespace is `SPRKD-EXT`. | Every candidate hash and release-gate assertion before that final namespace check passed, and the command wrote nothing. | Read the ledger's external section directly and rerun the same contiguity assertion against its exact `SPRKD-EXT-001` through `003` identifiers. |

## Upstream/release issues

| ID | Evidence | Consequence for replication |
|---|---|---|
| SPRKD-UPSTREAM-001 | README calls the 10-epoch command a full reproduction; paper specifies 500 epochs and five trials. | Exact advertised command cannot reproduce Table 1. We run 500 x five and retain the advertised-command distinction. |
| SPRKD-UPSTREAM-002 | `reproduce_malaria.py` never populates `teacher_best_val_acc`. | Weak-teacher performance must be independently evaluated and logged. |
| SPRKD-UPSTREAM-003 | The reproduction script directly initializes the trained SPRKD student at its ASR target, contrary to the paper's explicit iterative-approach description. | Report exact-script and narrowly corrected tracks separately. |
| SPRKD-UPSTREAM-004 | ASR injection mutates `teacher_models[0]`; that mutated object is later supplied to RKD. | Exact-script RKD is not RKD from the saved two-epoch weak teacher. Both variants are run and labeled. |
| SPRKD-UPSTREAM-005 | `aggregate_asr` chooses each repository's final snapshot while paper/docs say lowest-loss; no monotonicity check is enforced. | Exact code is preserved; saddle loss ordering is retained for audit. |
| SPRKD-UPSTREAM-006 | Released checkpoint, historical metric trace, and Table 1 do not identify a common recoverable trained state. | Artifact replay cannot independently verify 94.80%; scratch training is required. |
| SPRKD-UPSTREAM-007 | `epoch_validation_series(..., step_size=323)` strides a series that contains 108 validation minibatches per epoch. | Release notebook's displayed “final epoch” values are individual minibatches. We reconstruct epoch means with stride 108 and report sample-weighted metrics. |
| SPRKD-UPSTREAM-008 | Two `tests/test_paper.py` tests check only file existence, treat Git-LFS pointer text as a real artifact, and fail; the checkpoint tests correctly detect pointers. | Pointer-only checkout test result is 175 passed, 2 failed, 9 skipped. Failures are packaging/test-fixture handling, not algorithm outcomes. |
| SPRKD-UPSTREAM-009 | The paper describes standard logit matching, but both released malaria models terminate in Softmax and `train_response_kd` applies `log_softmax`/`softmax` to those probabilities again. | Tracks B/C preserve the released double-softmax baseline. It cannot by itself establish a comparison against conventional logit KD; that correction is labeled as a possible extension. |
| SPRKD-UPSTREAM-010 | The paper first says 4 student and 2–20 teacher eigenvalues, later says 2 student and 20 teacher; the reproduction path leaves the package default at 4 for both. | Track B preserves the executable default and reports it. The exact Table 1 eigenvalue configuration is not recoverable without author clarification. |
| SPRKD-UPSTREAM-011 | Released 500-epoch Hessian artifacts contain traces of 54.96 (SPRKD), 35.48 (Control-S), and 209.47 (RKD), not Table 1's 33.39, 71.33, and 408.27; the released SPRKD/Control-S ordering is also reversed. | The report distinguishes the reproducible artifact values from the paper values. Because stochastic-probe and exact-state provenance is incomplete, it does not claim which calculation is wrong. |
| SPRKD-UPSTREAM-012 | `sprkd.stats._exact_binomial_p_value` directly evaluates `comb(n, i) * 0.5**n`; full-validation discordant counts overflow when the huge integer coefficient is converted to floating point. | The released helper cannot calculate some of the full-split exact McNemar comparisons used by the paper. We preserve its contingency table and use SciPy's numerically stable exact binomial implementation, with small-count equivalence tests. |
| SPRKD-UPSTREAM-013 | Both malaria architectures terminate in `Softmax`, while supervised teacher, SPRKD, and control loops pass those probabilities to PyTorch `CrossEntropyLoss`, whose documented input is unnormalized logits. | Tracks B/C preserve the release exactly. Because completed seeds exhibit large transient and sometimes permanent accuracy collapses, a paired post-hoc diagnostic removes only the terminal activation; causality is not assumed in advance. |
| SPRKD-UPSTREAM-014 | The paper describes McNemar `p = 1.0` against Control-S as “statistical equivalence,” but a failure to reject equality is not an equivalence test and no equivalence margin is specified. | Treat the value, if reproducible, as evidence that the paired test did not detect a difference—not proof of practical equivalence. Report accuracy differences and uncertainty directly; an equivalence claim requires a prospectively justified margin and corresponding design. |
| SPRKD-UPSTREAM-015 | Table 1 labels each value as a five-trial average but releases no per-trial values, seeds, standard deviation, interval, or explicit failed-run handling. | The reported means cannot be used to assess training variance or distinguish stable convergence from checkpoint/run selection. Publish every independent seed and a descriptive fresh-training interval in the replication; request the original five records. |
| SPRKD-UPSTREAM-016 | The release performs a seeded random split over individual cell images and does not use the NLM patient-to-cell mapping to keep patients disjoint. | This reproduces the paper's disclosed 75/25 split, but does not establish patient-level generalization; cells from one patient can enter both partitions. A clinical-generalization extension should freeze a grouped patient split and report it separately. |

## External/operational limitations

| ID | What happened | Handling |
|---|---|---|
| SPRKD-EXT-001 | The Hugging Face paper endpoint returned 404 because this new arXiv record was not indexed there yet. | Acquired the PDF/source from arXiv and the code from the paper-linked GitHub repository. |
| SPRKD-EXT-002 | The current NULSPEC publication-contract validator and arm pages encode the prior study's reward-delta/prompt-interval metric shape rather than a generic quantitative-arm schema. | Do not mislabel classification accuracy as reward. Deliver a typed accuracy/run projection and frontend contract; canonical site import remains a website-schema task and does not gate the scientific replication result. |
| SPRKD-EXT-003 | The preferred GitHub app returned HTTP 403 when asked to open the draft PR after the branch had been pushed successfully. | The commit and remote branch were intact. Used the documented authenticated GitHub CLI fallback to create the same draft PR without changing study content. |
